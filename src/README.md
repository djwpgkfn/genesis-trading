# src/ — application composition root

Placeholder until **S8 (Production Engine)**, where the Cycle Orchestrator and Control/Data plane
wiring live. Engines are built as `packages/*` and composed here. No engine logic in S0.
